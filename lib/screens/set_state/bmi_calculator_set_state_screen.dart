import 'package:flutter/material.dart';

class BmiCalculatorSetStateScreen extends StatelessWidget {
  const BmiCalculatorSetStateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
